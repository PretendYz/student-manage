server:
  port: 8080

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/student_manage?serverTimezone=UTC&characterEncoding=utf8&useUnicode=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver

swagger:
  enable: true

mybatis-plus:
  type-aliases-package: com.hengtiansoft.studentManage.entity
  mapper-locations: classpath:mapper/*.xml
